import { StyleSheet } from 'react-native';

export const JSTopicScreenStyles = () => {
  return StyleSheet.create({
    container: {
      flex: 1,
      marginHorizontal: 16,
    },
    buttonContainer: {
      marginBottom: 16,
    },
  });
};
